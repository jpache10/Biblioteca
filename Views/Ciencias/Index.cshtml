@model PaginatedList<Biblioteca.Models.Ciencia>

@{
    ViewData["Title"] = "Ciencias";

    var columns = new List<ColumnConfig<Biblioteca.Models.Ciencia>>
    {
        new ColumnConfig<Biblioteca.Models.Ciencia> { Header = "#", ValueSelector = item => item.Identificador, CssClass = "text-center" },
        new ColumnConfig<Biblioteca.Models.Ciencia> { Header = "Descripción", ValueSelector = item => item.Descripcion },
        new ColumnConfig<Biblioteca.Models.Ciencia> { Header = "Estado", ValueSelector = item =>  $"<span class='{(item.Estado ? "green-circle" : "red-circle")}'></span> {(item.Estado ? "Activo" : "Inactivo")}"}
    };
}

@await Html.PartialAsync("_IndexHeader", ViewData["Title"].ToString())
@await Html.PartialAsync("TableElementCiencia", Tuple.Create(Model, columns))
