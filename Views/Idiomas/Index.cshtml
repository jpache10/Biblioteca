@model PaginatedList<Biblioteca.Models.Idioma>

@{
    ViewData["Title"] = "Idiomas";

    var columns = new List<ColumnConfig<Biblioteca.Models.Idioma>>
    {
        new ColumnConfig<Biblioteca.Models.Idioma> { Header = "#", ValueSelector = item => item.Identificador, CssClass = "text-center" },
        new ColumnConfig<Biblioteca.Models.Idioma> { Header = "Descripción", ValueSelector = item => item.Descripcion },
        new ColumnConfig<Biblioteca.Models.Idioma> { Header = "Estado", ValueSelector = item =>  $"<span class='{(item.Estado ? "green-circle" : "red-circle")}'></span> {(item.Estado ? "Activo" : "Inactivo")}"}
    };
}

@await Html.PartialAsync("_IndexHeader", ViewData["Title"].ToString())
@await Html.PartialAsync("TableElementIdioma", Tuple.Create(Model, columns))
